extends layout

block content
    if tag
        h1 Hello #{tag.name}!
        p Here are your full details:
        pre= JSON.stringify(tag, null, 2)
    else
        h1 There's no such tag!
    script.
        console.log(!{JSON.stringify(tag)})
